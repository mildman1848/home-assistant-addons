#include <tunables/global>

profile neolink flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  network inet stream,
  network inet6 stream,
  network inet dgram,
  network inet6 dgram,

  file,
  signal (send) set=(kill,term,int,hup,cont),

  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /usr/sbin/** ix,
  /usr/local/bin/neolink ix,
  /etc/cont-init.d/** ix,
  /etc/services.d/** ix,
  /etc/neolink.toml r,
  /data/options.json r,
  /config/** rw,
  /run/** rw,
  /tmp/** rw,
}
